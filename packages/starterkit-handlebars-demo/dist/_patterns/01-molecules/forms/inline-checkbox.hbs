<label class="c-input-group {{ styleModifier }}" for="{{ id }}">

	<input id="{{ id }}" type="checkbox" name="{{ name }}" value="{{ value }}" class="c-input-group__control" {{#if checked}}checked{{/ if }}>
	<span class="c-input-group__text">{{ label }}</span>

</label><!-- end c-input-group -->
